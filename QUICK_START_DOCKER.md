# 🚀 Быстрый запуск через Docker

## ✅ ШАГ 1: Установите Docker Desktop

1. Скачайте: https://www.docker.com/products/docker-desktop/
2. Установите (нажимайте "Далее")
3. **ПЕРЕЗАГРУЗИТЕ КОМПЬЮТЕР**
4. Запустите Docker Desktop
5. Дождитесь зеленой иконки внизу (Docker Engine running)

## ✅ ШАГ 2: Проверьте, что Docker работает

Откройте PowerShell и выполните:

```powershell
docker version
```

Должны увидеть версию Docker.

## ✅ ШАГ 3: Запустите бота

В папке проекта выполните:

```powershell
docker compose up -d
```

⚠️ **ВАЖНО:** В новой версии Docker это `docker compose` (с пробелом), а не `docker-compose`!

## ✅ ШАГ 4: Проверьте, что всё работает

```powershell
# Смотрим статус контейнеров
docker compose ps

# Смотрим логи
docker compose logs -f
```

Должны быть запущены 4 контейнера:
- ✅ bot_postgres
- ✅ bot_parser
- ✅ bot_user
- ✅ bot_admin

## 📱 Проверка бота

Напишите вашему боту в Telegram любое сообщение - он должен ответить меню.

---

## 🛠️ Полезные команды

```powershell
# Остановить все контейнеры
docker compose down

# Перезапустить
docker compose restart

# Смотреть логи конкретного сервиса
docker compose logs -f user_bot

# Пересобрать и запустить заново
docker compose up -d --build

# Удалить всё (включая данные)
docker compose down -v
```

---

## ❌ Если что-то не работает

### Docker не запускается
- Убедитесь, что WSL 2 включен
- Перезапустите Docker Desktop
- Перезагрузите компьютер

### Контейнеры не стартуют
```powershell
# Смотрим что случилось
docker compose logs

# Пересобираем
docker compose down
docker compose up -d --build
```

### Бот не отвечает
```powershell
# Смотрим логи пользовательского бота
docker compose logs user_bot

# Проверяем, что токен правильный в .env файле
```

---

## 🎯 Быстрая шпаргалка

```powershell
# Запустить
docker compose up -d

# Остановить
docker compose down

# Логи
docker compose logs -f

# Статус
docker compose ps

# Перезапустить один сервис
docker compose restart user_bot
```

---

✅ **Всё готово!** Файл `.env` уже создан с вашими токенами.
